import { SearchImageTitlePipe } from './search-image-title.pipe';

describe('SearchImageTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchImageTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
